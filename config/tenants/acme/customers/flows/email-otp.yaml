description: ''
start: init
nodes:
  init:
    name: init
    use: init
    custom_config: {}
    next:
      start: emailOTP

  emailOTP:
    name: emailOTP
    use: emailOTP
    custom_config:
      init_user: true
      ask_email: true
    next:
      success-registered-email: successResult
      success-unkown-email: successResult
  
  failureResult:
    name: failureResult
    use: failureResult
    custom_config:
      message: "Failed to login with Email OTP"
    next: {}
    
  successResult:
    name: successResult
    use: successResult
    custom_config:
      message: Login with Email OTP successful
    next: {}